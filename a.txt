begin
  b := 2;
  a := @sqrt(64) * 2 - 3.5;
  d:= @max(a,b);
  write(d);
  c:= a = b | false;
  write(c);
  write(b);
  write(a);
  b:= 8;
  while (b > 0) do
  begin
  b := b - 1;
    write(b)
  end
end